{
  "scripts": {
    "start": "npm-run-all --parallel live-server sass",
    "live-server": "live-server --open=./",
    "sass": "sass --watch styles/style.scss:styles/style.css --style=expanded"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "sass": "^1.78.0",
    "live-server": "^1.2.2"
  }
}